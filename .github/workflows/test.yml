name: Test
on:
  push:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: test
        run: |
          #source script.sh
          whoami
          pwd
          cat /etc/ssh/sshd_config
