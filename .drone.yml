---
kind: pipeline
type: docker
name: test

platform:
  os: linux
  arch: amd64

steps:
- name: check
  image: x0rzavi/gentoo-kernel:latest
  commands:
  - df -h
  - free -h
  - pwd
  - lsblk
  - cat /proc/cpu | head -n 50
  - uname -a
