---
kind: pipeline
type: docker
name: build kernel

platform:
  os: linux
  arch: amd64

steps:
- name: check
  image: x0rzavi/gentoo-kernel:latest
  commands:
    - source ./build.sh
    - source ./upload.sh
